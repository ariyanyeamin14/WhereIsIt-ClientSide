import React from 'react';
import Slider from '../../components/Slider';

const Banner = () => {
    return (
        <div>
            <Slider></Slider>
        </div>
    );
};

export default Banner;